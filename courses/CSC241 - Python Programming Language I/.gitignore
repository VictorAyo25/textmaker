# Regenerable from the source PDF; see README "Rebuilding".
sources/extracted/manual_text/

# Build outputs, all regenerated by `cd build && python assemble.py`.
# The tracked source of truth is build/content/*.html plus manual.css.
build/.build.lock
build/__pycache__/
build/_body.pdf
build/_cover.pdf
build/cover_page.html
build/CSC241_checkpoint.pdf
