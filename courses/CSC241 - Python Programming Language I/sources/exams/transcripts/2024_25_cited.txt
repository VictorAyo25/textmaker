CSC241 2024/2025 OMEGA SEMESTER EXAMINATION
PARTIAL verbatim transcript, read by eye from sources/exams/exam_2024_25.pdf
(a pure scan: all six pages carry a zero-length text layer, so there was no OCR
to reject, and page 3 was rendered at 210 dpi and read).

SCOPE: PARTIAL

This paper is NOT solved in the book. Only the fragments below are quoted, in
teaching boxes that cite them, so only the paraphrase direction of qa_verbatim
applies here. The coverage direction (every question in the transcript must be
quoted in the book) is deliberately NOT run against a PARTIAL file: it would
demand the whole 2024/2025 paper be reproduced, which is not what the book does.

Add to this file whenever the book starts citing another part of this paper.

=== Question Two (e), requirement 4, page 3 ===

4. Display:
- The sorted list of scores,
- The highest and lowest scores,
- The average score,
- The number of passes and fails.

=== END ===

TRANSCRIBER'S NOTES (not part of the paper)

1. Immediately beneath requirement 4, in HANDWRITING and not in the printed
   text, the paper reads:

     Pass mark is 45 and above

   The same annotation appears again on page 5 beside Question Five (e). It is
   handwriting, so it is recorded here as an annotation and is never presented
   in the book as part of the printed question. This is the whole point of the
   trap the book teaches: the printed paper never defines the threshold, so
   requirement 4 is unanswerable as printed without the note.
2. The requirement list uses a HYPHEN as its bullet, measured against the
   hyphen in "user-defined" on the same page.
